class ApiService {
    startSimulation() {
        // Placeholder: would start websocket or polling in real app
        console.log('Simulation started');
    }
}

export const apiService = new ApiService();
